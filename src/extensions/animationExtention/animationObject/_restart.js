///////////////////////////
// Restart the animation
// (set _currenFrame = -1)
///////////////////////////
mge._extensionAnimation._animationObject._restart = function() {

    // Restart the animation
    this._currentFrame=-1
}